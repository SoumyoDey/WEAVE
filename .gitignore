node_modules/
.DS_Store
build/
.env
Data/.env
*_backup.sql

# Python
__pycache__/
*.pyc
.venv/
venv/

# Server-side plot cache (Data/flask_api.py FileSystemCache backend)
.cache/

# Local Claude Code config (dev-server launch settings — machine-specific)
.claude/

# Coverage artifacts (pytest-cov)
.coverage
coverage.xml
htmlcov/

# Raw model files kept locally for checking ingest conventions against source
# (GRIB/NetCDF, tens of MB each). Useful to have on hand, too big to version —
# see METRICS_AUDIT.md finding 16 for what they were used to verify.
test_data/
*.nc
*.grib
*.grib2
